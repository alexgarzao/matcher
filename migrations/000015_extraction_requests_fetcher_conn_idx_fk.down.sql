-- Reserved migration kept intentionally as a no-op.
-- The planned extraction_requests index/FK work was absorbed into 000013
-- before release, so this slot remains to preserve migration numbering.
SELECT 1;
